
declare module '{{module}}' {

  {{#if extends}}
  import SuperClass from "{{{formatNameSpaceToModule extends}}}"
  {{/if}}

  /**
    * {{{formatDescription description}}}
    * @see [document](https://openui5.hana.ondemand.com/#/api/{{{name}}})
    */
  export {{formatDefault module basename}}class {{formatBaseName basename}} {{#if extends}}extends SuperClass{{/if}} {

    {{#each methods}}
    /**
      * {{{formatDescription description}}}
      {{#if href}}* @see [document](https://openui5.hana.ondemand.com/{{href}}{{/if}})
      * @since {{since}}
      */
    {{#if static}} static{{/if}} {{{formatClassMethodName name}}}({{#each parameters}}{{#unless phoneName}}{{{name}}}, {{/unless}}{{/each}}...objects);

    {{/each}}

  }

}
